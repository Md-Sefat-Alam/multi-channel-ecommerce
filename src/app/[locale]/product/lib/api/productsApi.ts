import { apiSlice } from "@/lib/api/apiSlice";
import { IOrder, IPaymentResponse } from "../types/productsTypes";

export const productsApi = apiSlice.injectEndpoints({
  endpoints: (builder) => ({}),
});

export const {} = productsApi;
